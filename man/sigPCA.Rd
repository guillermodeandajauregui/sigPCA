% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sigPCA.R
\name{sigPCA}
\alias{sigPCA}
\title{Unified PCA Significance Wrapper}
\usage{
sigPCA(
  data,
  method = c("mp", "perm", "both"),
  num_permutations = 1000,
  center = TRUE,
  scale. = TRUE
)
}
\arguments{
\item{data}{A numeric matrix or data frame.}

\item{method}{One of \code{"mp"}, \code{"perm"}, or \code{"both"}. Default: \code{"mp"}.}

\item{num_permutations}{Integer. Used only if \code{method} is \code{"perm"} or \code{"both"}.}

\item{center, scale.}{Logical. Passed to \code{scale()}. Default: TRUE.}
}
\value{
A structured list containing method results.
}
\description{
Runs significance testing on PCA eigenvalues using either Marchenko–Pastur
bounds, permutation tests, or both. Results are returned as a structured list.
}
\examples{
set.seed(123)
x <- matrix(rnorm(100 * 10), nrow = 100, ncol = 10)
sigPCA(x, method = "mp")
sigPCA(x, method = "perm", num_permutations = 100)
sigPCA(x, method = "both", num_permutations = 100)
}
